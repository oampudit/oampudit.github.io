; Several deps (e.g. react-github-calendar@3) still declare a React 17 peer
; while this app runs React 18. They work fine in practice, so accept the
; peer mismatch the way the project has always been installed. Without this,
; `npm ci` on CI fails with ERESOLVE and the Pages deploy never builds.
legacy-peer-deps=true
